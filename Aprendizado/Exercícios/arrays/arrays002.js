let n = [1,2,3,4,5]

/*for (let pos = 0; pos < n.length; pos ++){
    console.log(`A posição ${pos} tem o valor ${n[pos]}`)
}

/* Método mais simples */

for (let pos in n){
    console.log(`A posição ${pos} tem o valor ${n[pos]} `)
}